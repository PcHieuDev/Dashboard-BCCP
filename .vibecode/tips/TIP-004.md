# TIP-004: Frontend — HCC Revenue Page + Routing /bccp + Xóa PDF

## HEADER
- TIP-ID: TIP-004
- Project: Dashboard-BCCP
- Module: Frontend — Routing, HCC, PDF Cleanup
- Depends on: TIP-002 (format "-" cần fix trước)
- Priority: P1
- Estimated effort: 30 minutes

## CONTEXT
- Working directory: `E:\Projects\Dashboard-BCCP`
- Key files to reference:
  - `dash_app/pages/revenue_detail.py` — Layout mẫu để clone cho HCC
  - `dash_app/callbacks/revenue_callbacks.py` — Callback mẫu + nơi xóa PDF logic
  - `dash_app/callbacks/export_helpers.py` — Hàm PDF cần xóa (L246-468) + imports (L17-23)
  - `dash_app/app.py` — Routing `/bccp/kpi` cần đổi thành `/bccp`
  - `dash_app/components/sidebar.py` — Menu cần cập nhật
  - `dash_app/requirements.txt` — Xóa reportlab, pdfkit
- Patterns to follow: context.md Section 5 — Page layout pattern, Callback pattern

## TASK
### 4A: Xóa toàn bộ PDF
1. `dash_app/pages/revenue_detail.py` L64: Xóa `dbc.Button("📄 Xuất PDF", id="revenue-btn-export-pdf", ...)`.
2. `dash_app/callbacks/revenue_callbacks.py`:
   - Xóa `Input("revenue-btn-export-pdf", "n_clicks")` khỏi callback.
   - Xóa logic `elif trigger_id == "revenue-btn-export-pdf"` + gọi `generate_pdf_report()`.
   - Sửa guard check `trigger_id not in (...)` bỏ "revenue-btn-export-pdf".
3. `dash_app/callbacks/export_helpers.py`:
   - Xóa `import reportlab` block (L17-23) + hàm font registration.
   - Xóa hàm `generate_pdf_report()` (L246-468, ~222 dòng).
4. `dash_app/requirements.txt`: Xóa dòng `reportlab>=4.0.0` và `pdfkit>=1.0.0`.

### 4B: Routing /bccp
1. `dash_app/app.py`: Đổi route `/bccp/kpi` → `/bccp`. Giữ nguyên layout KPI.
2. `dash_app/components/sidebar.py`: Đổi link menu BCCP KPI từ `/bccp/kpi` → `/bccp`.

### 4C: Tạo trang HCC Revenue
1. Tạo file mới `dash_app/pages/hcc_revenue.py`:
   - Clone layout từ `revenue_detail.py` (dropdown Group By, radio So sánh, nút Xuất Excel, Spinner + table container).
   - Đổi tất cả `id` thành prefix `hcc-revenue-` (ví dụ: `hcc-revenue-g1`, `hcc-revenue-g2`, `hcc-revenue-compare-opt`, `hcc-revenue-btn-export-excel`, `hcc-revenue-download`, `hcc-revenue-table-container`).
   - **KHÔNG có nút PDF**.
2. Tạo file mới `dash_app/callbacks/hcc_revenue_callbacks.py`:
   - Clone logic từ `revenue_callbacks.py` (chỉ phần Excel, bỏ PDF).
   - Thay query filter: thêm `WHERE d.nhom_chinh = 'HCC'` (JOIN dim_dichvu).
   - Đổi tất cả callback Input/Output id sang prefix `hcc-revenue-`.
3. `dash_app/app.py`: Thêm route `/hcc/revenue` → layout `create_hcc_revenue_layout()`.
4. `dash_app/components/sidebar.py`: Thêm link "Báo cáo doanh thu" dưới menu Hành chính công → `/hcc/revenue`.
5. Import và register callbacks mới trong `app.py`.

## SPECIFICATIONS
- Trang HCC Revenue phải dùng chung `GROUP_BY_LABEL_MAP` và `render_revenue_datatable` từ `data_table.py`.
- Query HCC Revenue phải filter theo `dim_dichvu.nhom_chinh = 'HCC'` — KHÔNG hardcode mã dịch vụ.
- Giữ nguyên cột Sản lượng, Khối lượng, Cước CB, Cước TT, Cước gồm VAT, Số KH.

## ACCEPTANCE CRITERIA
```
Given: Toàn bộ codebase
When: Grep "pdf" hoặc "PDF" hoặc "btn-export-pdf" trong dash_app/
Then: Không tìm thấy instance nào (trừ comment nếu có)

Given: Mở URL /bccp
When: Trang load xong
Then: Hiển thị layout KPI (7 thẻ KPI + sparkline) — giống /bccp/kpi cũ

Given: Sidebar > Hành chính công > Báo cáo doanh thu
When: Click vào menu
Then: Chuyển đến /hcc/revenue, hiển thị bộ lọc Group By + bảng DataTable
      Dữ liệu chỉ chứa giao dịch có nhom_chinh='HCC'

Given: Trang /hcc/revenue
When: Chọn Xuất Excel
Then: File Excel tải về chứa đúng dữ liệu HCC
```

## CONSTRAINTS
- KHÔNG thay đổi logic của các trang BCCP hiện có (kpi, revenue, customer, charts, alerts)
- KHÔNG sửa file analytics/revenue.py (query engine)
- KHÔNG thêm dependency mới
- Phải reuse `render_revenue_datatable` từ data_table.py, KHÔNG tạo hàm mới
