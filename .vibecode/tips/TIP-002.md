# TIP-002: Backend — Fix ten_cum + Cache + Format "-"

## HEADER
- TIP-ID: TIP-002
- Project: Dashboard-BCCP
- Module: Backend Analytics
- Depends on: TIP-001 (khuyên dùng — dim_dichvu cần có nhom_chinh đúng)
- Priority: P0
- Estimated effort: 25 minutes

## CONTEXT
- Working directory: `E:\Projects\Dashboard-BCCP`
- Key files to reference:
  - `analytics/global_metrics.py` — Hàm `get_revenue_by_cum` bị lỗi `ten_Cum`
  - `dash_app/components/data_table.py` — Hàm `render_revenue_datatable` format `pct_change`
  - `dash_app/callbacks/utils.py` — Ví dụ cache pattern (`run_query_cached`)
- Patterns to follow: context.md Section 5 — Cache pattern, DataTable pattern

## TASK
1. **Fix `analytics/global_metrics.py`**:
   - Tìm tất cả instance `ten_Cum` (chữ C hoa) → đổi thành `ten_cum` (chữ c thường).
   - Thêm caching cho hàm `get_revenue_by_cum` (hoặc hàm tương đương tính YTD theo Cụm). Dùng dict-based cache với TTL 300 giây (5 phút).
   - Khi tính Tỷ lệ hoàn thành (%): Nếu Kế hoạch = 0 hoặc None → trả về chuỗi `"-"` thay vì gây lỗi chia cho 0.

2. **Fix `dash_app/components/data_table.py`**:
   - Trong hàm `render_revenue_datatable`, tại đoạn format `pct_change`:
   - Hiện tại: `lambda x: f"{x:+.1f}%" if pd.notna(x) and x != 0 else "-"`
   - Kiểm tra và đảm bảo logic: Nếu giá trị là 0, NaN, None, hoặc Inf → hiển thị `"-"`.

## SPECIFICATIONS
- Cache phải có khả năng clear (gọi từ sync_mappings hoặc import).
- Hàm clear cache phải được export để TIP-005 gọi sau khi sync danh mục.
- Không thay đổi signature của các hàm public hiện tại.

## ACCEPTANCE CRITERIA
```
Given: Database có dữ liệu transactions + dim_buucuc + dim_dichvu
When: Gọi hàm get_revenue_by_cum (hoặc tương đương)
Then:
  - DataFrame trả về có cột ten_cum (không lỗi KeyError)
  - Dữ liệu 18 Cụm có số liệu (không rỗng)
  - Lần gọi thứ 2 trong vòng 5 phút trả về từ cache (nhanh hơn)

Given: Một Cụm có Kế hoạch = 0
When: Tính Tỷ lệ hoàn thành
Then: Cột % hiển thị "-" thay vì "Inf%" hoặc crash

Given: Kỳ trước không có dữ liệu (so sánh prev_period)
When: Render DataTable với compare_opt = "prev_period"
Then: Cột pct_change hiển thị "-" thay vì "0%" hoặc "+0.0%"
```

## CONSTRAINTS
- KHÔNG đổi tên hàm public nào trong global_metrics.py
- KHÔNG thay đổi cấu trúc cột trả về (chỉ fix giá trị)
- KHÔNG dùng Redis/Memcached — chỉ in-memory cache
- KHÔNG sửa file analytics/revenue.py (ngoài scope TIP này)
