# TIP-010: Import UI — Thêm option PHBC vào trang Import

## HEADER
- TIP-ID: TIP-010
- Project: Dashboard-BCCP Phase 7
- Module: Frontend — Import Page
- Depends on: TIP-009 (hàm import_phbc_excel phải tồn tại)
- Priority: P1
- Estimated effort: 20 minutes

## CONTEXT
- Working directory: `E:\Projects\Dashboard-BCCP`
- Key files:
  - `dash_app/pages/import_data.py` — Dropdown loại dữ liệu (`import-service-type`)
  - `dash_app/callbacks/import_callbacks.py` — Logic xử lý upload theo loại

## TASK

### Bước 1: Sửa `dash_app/pages/import_data.py`

Tìm `dcc.Dropdown(id="import-service-type", ...)`. Thêm option PHBC vào danh sách:

```python
# Thêm sau option BCCP:
{"label": "📰 Phát hành báo chí (PHBC)", "value": "PHBC"},
```

Danh sách cuối cùng sẽ là:
1. Giao dịch BCCP
2. **📰 Phát hành báo chí (PHBC)** ← Thêm mới
3. Giao dịch HCC
4. Giao dịch TCBC
5. Giao dịch PPBL
6. Kế hoạch chỉ tiêu

### Bước 2: Sửa callback hướng dẫn format trong `import_callbacks.py`

Tìm callback cập nhật `import-format-instructions` theo `import-service-type`. Thêm case PHBC:

```python
elif service_type == "PHBC":
    return dbc.Alert([
        html.Strong("📰 Phát hành báo chí (PHBC)"),
        html.Br(),
        "Yêu cầu các cột: ",
        html.Code("ma_buu_cuc"), ", ",
        html.Code("thang_du_lieu"), " (số 1-12), ",
        html.Code("nam_du_lieu"), ", ",
        html.Code("doanh_thu"),
        html.Br(),
        html.Small("Dữ liệu sẽ được nạp vào bảng transactions_phbc.")
    ], color="warning", className="mt-2")
```

### Bước 3: Sửa callback xử lý upload trong `import_callbacks.py`

Tìm callback `confirm_upload` (hoặc tương tự) xử lý khi user bấm "Xác nhận nạp". Thêm case PHBC:

```python
elif service_type == "PHBC":
    from etl.importer import import_phbc_excel
    result = import_phbc_excel(DB_PATH, temp_file_path)
```

## SPECIFICATIONS
- Option PHBC xuất hiện trong dropdown với icon 📰
- Hướng dẫn format hiện rõ 4 cột cần có
- Logic import gọi đúng `import_phbc_excel` từ `etl/importer.py`
- Nếu import thành công → Alert xanh với số dòng đã nạp
- Nếu import lỗi → Alert đỏ với message lỗi

## ACCEPTANCE CRITERIA
```
Given: Trang /import
When: User mở dropdown "Loại dữ liệu nạp"
Then: Thấy option "📰 Phát hành báo chí (PHBC)" trong danh sách ✅

Given: User chọn PHBC trong dropdown
When: Dropdown thay đổi
Then: Hiện hướng dẫn format với 4 cột cần có ✅

Given: User upload file Excel PHBC đúng format
When: Bấm "Xác nhận nạp"
Then: Alert xanh "✅ Đã nạp X dòng dữ liệu PHBC thành công" ✅
      Dữ liệu xuất hiện trong transactions_phbc ✅

Given: User upload file PHBC thiếu cột bắt buộc
When: Bấm "Xác nhận nạp"
Then: Alert đỏ mô tả lỗi cụ thể, KHÔNG crash app ✅
```

## CONSTRAINTS
- KHÔNG thay đổi logic import của các loại dữ liệu khác (BCCP, HCC, TCBC, PPBL, Plans)
- KHÔNG thay đổi UI layout của trang Import
- Chỉ thêm 1 option vào dropdown + 1 case vào 2 callbacks
- Surgical changes only
