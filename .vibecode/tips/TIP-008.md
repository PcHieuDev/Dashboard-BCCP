# TIP-008: BCCP KPI — Thay thẻ HCC bằng thẻ PHBC

## HEADER
- TIP-ID: TIP-008
- Project: Dashboard-BCCP Phase 7
- Module: BCCP KPI Page
- Depends on: TIP-009 (bảng transactions_phbc phải tồn tại trước)
- Priority: P1
- Estimated effort: 30 minutes

## CONTEXT
- Working directory: `E:\Projects\Dashboard-BCCP`
- Key files:
  - `dash_app/pages/kpi_page.py` — Layout 7 KPI cards hiện tại
  - `dash_app/callbacks/kpi_callbacks.py` — Logic query + render 7 cards
- Tình trạng hiện tại:
  - Card 3 trong `/bccp` hiện là "Hành chính công" (HCC) — SAI vì HCC không thuộc BCCP
  - PHBC (Phát hành báo chí) là dịch vụ BCCP, dữ liệu nằm trong `transactions_phbc` (sau TIP-009)

## TASK

### Bước 1: Sửa `dash_app/pages/kpi_page.py`

Tìm card HCC trong layout (grep `kpi-hcc-value`). Thay toàn bộ card HCC thành card PHBC:
- ID cũ: `kpi-hcc-value`, `kpi-hcc-sparkline`, `kpi-hcc-delta-prev`, `kpi-hcc-delta-yoy`
- ID mới: `kpi-phbc-value`, `kpi-phbc-sparkline`, `kpi-phbc-delta-prev`, `kpi-phbc-delta-yoy`
- Tên hiển thị: "📰 Phát hành báo chí" (thay "🏢 Hành chính công")
- Màu card: giữ màu cũ hoặc dùng `#F97316` (cam) cho PHBC

### Bước 2: Sửa `dash_app/callbacks/kpi_callbacks.py`

**2a. Sửa danh sách Output** (L40-41): thay `kpi-hcc-*` → `kpi-phbc-*`

**2b. Sửa logic tính toán** (L180-188):

```python
# TRƯỚC (HCC tính từ transactions cùng BCCP):
hcc_cur = get_revenue_by_nhom(df_cur, 'Hành chính công')
hcc_prev = get_revenue_by_nhom(df_prev, 'Hành chính công')
hcc_yoy = get_revenue_by_nhom(df_yoy, 'Hành chính công')
bccp_cur = tot_cur - hcc_cur  # BCCP = Tổng - HCC (logic sai)

# SAU (PHBC query riêng từ transactions_phbc):
phbc_cur = _get_phbc_revenue(DB_PATH, year, month_val)
phbc_prev = _get_phbc_revenue(DB_PATH, pyear, pmonth)  # cần tính pyear/pmonth
phbc_yoy = _get_phbc_revenue(DB_PATH, year - 1, month_val)
bccp_cur = tot_cur  # BCCP = toàn bộ transactions (HCC đã tách sang dim_dichvu)
```

**2c. Thêm helper function** `_get_phbc_revenue(db_path, year, month)`:
```python
def _get_phbc_revenue(db_path, year, month):
    """Query doanh thu PHBC từ bảng transactions_phbc. Trả về 0 nếu bảng chưa tồn tại."""
    try:
        conn = sqlite3.connect(str(db_path))
        thang = f"T{month:02d}"
        df = pd.read_sql_query(
            "SELECT SUM(doanh_thu) as dt FROM transactions_phbc WHERE nam_du_lieu=? AND thang_du_lieu=?",
            conn, params=[year, thang]
        )
        conn.close()
        return float(df.iloc[0]['dt'] or 0)
    except Exception:
        return 0.0
```

**2d. Sửa render** (L265): thay `hcc_n, hcc_s, hcc_p, hcc_y` → `phbc_n, phbc_s, phbc_p, phbc_y`

**2e. Sửa return** (L274): thay `hcc_n, hcc_s, hcc_p, hcc_y` → `phbc_n, phbc_s, phbc_p, phbc_y`

## SPECIFICATIONS
- `transactions_phbc` chưa có data → card PHBC hiện "0 đ" — KHÔNG crash
- BCCP card = `tot_cur` (tổng transactions, HCC đã tách ra khỏi dim_dichvu → không còn lẫn)
- Sparkline PHBC: dùng màu `#F97316` (cam)
- Helper `_get_phbc_revenue` đặt trong `kpi_callbacks.py` (không phải trong analytics/)

## ACCEPTANCE CRITERIA
```
Given: Trang /bccp load
When: Chưa có file PHBC nào được import (bảng transactions_phbc rỗng)
Then: Card PHBC hiện "0 đ", không crash, không lỗi console ✅

Given: Trang /bccp load
When: Có dữ liệu PHBC trong transactions_phbc
Then: Card PHBC hiển thị đúng doanh thu PHBC của tháng đang xem ✅

Given: Trang /bccp load
When: Nhìn vào 7 KPI cards
Then: KHÔNG còn card "Hành chính công" trong /bccp ✅
      Thấy card "Phát hành báo chí" ở vị trí card thứ 3 ✅
```

## CONSTRAINTS
- KHÔNG sửa logic 4 card còn lại (Tổng, BCCP, TT, TMĐT, Quốc tế, KH)
- KHÔNG đổi số lượng card (vẫn 7 cards)
- KHÔNG gọi `query_revenue` cho PHBC (cấu trúc bảng khác)
- `_get_phbc_revenue` phải có try/except — bảng có thể chưa tồn tại
