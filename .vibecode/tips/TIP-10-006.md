# TIP-10-006: Nâng cấp /bccp/new-customer — Leaderboard + Top KHM + Bar DV

## Header
- **TIP-ID**: TIP-10-006
- **Trang**: `/bccp/new-customer`
- **Files**: `pages/new_customer.py`, `callbacks/new_customer_callbacks.py`
- **Priority**: P1 | **Effort**: Medium

## Yêu cầu PRD
1. **Leaderboard Cụm**: Bảng xếp hạng 18 Cụm theo SL KHM + DT KHM
2. **Top KHM giá trị cao**: Bảng Top CMS bán mới có DT cao nhất
3. **Bar Chart DV**: Biểu đồ thanh phân rã DV mà KHM sử dụng

## Chi tiết

### 1. Leaderboard Cụm (thay thế hoặc bổ sung bảng BĐX hiện có)
```python
# Data: Query new_customers JOIN dim_buucuc GROUP BY ten_cum
# Cột: Hạng | Cụm | SL KHM | DT bán mới | % KH toàn tỉnh
dash_table.DataTable(
    id="new-cust-leaderboard",
    columns=[
        {"name": "#", "id": "rank"},
        {"name": "Cụm", "id": "ten_cum"},
        {"name": "SL KHM", "id": "sl_khm", "type": "numeric"},
        {"name": "DT bán mới", "id": "dt_khm", "type": "numeric"},
        {"name": "% Toàn tỉnh", "id": "pct_total", "type": "numeric"},
    ],
    style_data_conditional=[
        {"if": {"row_index": [0,1,2]}, "fontWeight": "bold", "backgroundColor": "#ECFDF5"},  # Top 3 xanh
    ]
)
```

### 2. Top KHM giá trị cao
```python
# Query: new_customers WHERE thang = current JOIN transactions 
# GROUP BY cms → SUM(cuoc_tt_tong) → ORDER BY DESC → LIMIT 10
# Cột: CMS | Bưu cục | DT tháng đầu | Nhóm DV chính
```

### 3. Bar Chart DV KHM sử dụng
```python
# Query: transactions WHERE cms IN (danh sách KHM tháng này)
# GROUP BY nhom_dich_vu → SUM
fig_bar_dv = px.bar(
    df_khm_dv, x='nhom_dv', y='cuoc_tt_tong',
    title="Phân rã DV mà KHM sử dụng",
    color='nhom_dv',
    color_discrete_map={"Truyền thống": "#2196F3", "TMĐT": "#4CAF50", "Quốc tế": "#FF9800"}
)
```

### Layout mới
```python
# ROW 1: 3 KPI + 2 Service (giữ nguyên)
# ROW 2: Leaderboard Cụm (Col lg=7) + Bar Chart DV (Col lg=5)
# ROW 3: Bảng BĐX chi tiết (giữ nguyên)
# ROW 4: Top KHM giá trị cao (Col lg=12)
```

## Acceptance Criteria
- Leaderboard 18 Cụm, Top 3 highlight xanh
- Top 10 KHM CMS có DT + nhóm DV chính
- Bar chart hiện phân rã DV (TT, TMĐT, QT)
- Nút Xuất Excel vẫn hoạt động
