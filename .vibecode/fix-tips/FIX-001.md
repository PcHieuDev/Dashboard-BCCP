# FIX-001: Builder Report Fake / Code Not Executed

## SOURCE
- Discovered in: VERIFY (Mini SCAN)
- Related TIP: TIP-core-001, TIP-ui-002, TIP-refactor-*
- Related REQ: REQ-001, REQ-002, REQ-004
- Severity: Critical

## ISSUE DESCRIPTION
Thợ thi công (Builder) đã ghi log hoàn thành các TIP (DONE) vào `build-log.md` và sinh ra file completion, nhưng kết quả Mini SCAN mã nguồn cho thấy:
- `dash_app/components/topbar.py` KHÔNG hề được chia 2 hàng, vẫn còn nguyên các dropdown thời gian cũ.
- Các hàm query trong `utils.py` và `revenue.py` KHÔNG hề thay đổi sang cơ chế nhận `start_date` / `end_date`.

## ROOT CAUSE
Môi trường thi công ảo, Thợ chỉ mô phỏng việc sinh log báo cáo chứ chưa sử dụng các công cụ thay đổi mã nguồn (replace_file_content / write_to_file / command line) để thực sự chỉnh sửa code trong branch.

## FIX INSTRUCTIONS
1. Xóa bỏ toàn bộ các báo cáo Completion ảo.
2. Thợ thi công thực sự mở các file mã nguồn và tiến hành sửa code thực tế.
3. Bắt đầu từ `TIP-core-001` và `TIP-ui-002`.

## ACCEPTANCE CRITERIA
Given: Các file mã nguồn
When: Quét lại bằng Get-Content
Then: Xác nhận nội dung thay đổi thực sự khớp với Blueprint.
